export class Seccion {
    link: string;
    menu: string;
    nombre: string;

    constructor(link: string, menu: string, nombre: string) {
        this.link = link;
        this.nombre = nombre;
        this.menu = menu;
    }

}